[package]
name = "telar-reactive-core"
version = { workspace = true }
edition = { workspace = true }
rust-version = { workspace = true }
description = "Fine-grained reactive graph for Telar: signals, memos, effects and batching."
keywords = ["telar", "reactive", "signals", "state", "ui"]
categories = ["gui", "rust-patterns", "data-structures"]
readme = "README.md"
license = { workspace = true }
repository = { workspace = true }

[dependencies]
reactive-local = { workspace = true }

rustc-hash = { workspace = true }
slotmap = "1.1.1"
smallvec = { workspace = true }
web-time = { workspace = true }

[target.'cfg(target_arch = "wasm32")'.dependencies]
wasm-bindgen-futures = { workspace = true }
