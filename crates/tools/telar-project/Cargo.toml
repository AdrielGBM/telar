[package]
name = "telar-project"
version = { workspace = true }
edition = { workspace = true }
rust-version = { workspace = true }
description = "The shape of a Telar project: source discovery, output paths, naming, and the build artifacts a transpile leaves behind."
keywords = ["telar", "build", "codegen", "gui"]
categories = ["development-tools::build-utils", "gui"]
readme = { workspace = true }
license = { workspace = true }
repository = { workspace = true }

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
toml = { workspace = true }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(docsrs)'] }
