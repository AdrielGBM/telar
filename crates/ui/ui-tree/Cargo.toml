[package]
name = "telar-ui-tree"
version = { workspace = true }
edition = { workspace = true }
rust-version = { workspace = true }
description = "Component and render-node tree for Telar, with event dispatch and overlay routing."
keywords = ["telar", "ui", "gui", "tree", "components"]
categories = ["gui", "data-structures"]
readme = "README.md"
license = { workspace = true }
repository = { workspace = true }

[dependencies]
geometry-core = { workspace = true }
platform-core = { workspace = true }
reactive-core = { workspace = true }
renderer-core = { workspace = true }

web-time = { workspace = true }

[dev-dependencies]
motion-core = { workspace = true }

criterion = { workspace = true }

[[bench]]
name = "tree_flatten"
harness = false
