[package]
name = "sandbox"
version = "0.1.0"
edition = "2024"

[dependencies]
rsx = { workspace = true, features = ["runtime"] }
tracing-subscriber = { version = "0.3.23", features = ["env-filter"] }
